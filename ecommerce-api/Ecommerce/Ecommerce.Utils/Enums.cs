namespace Ecommerce.Utils
{
    public static class Enums
    {
        public enum TokenTypes
        {
            ConfirmToken = 1,
            ForgotToken = 2,
            InvitedToken = 3
        }

        public enum DataSourceTypes
        {
            Excel = 1,
            Connector = 2,
        }
    }
}